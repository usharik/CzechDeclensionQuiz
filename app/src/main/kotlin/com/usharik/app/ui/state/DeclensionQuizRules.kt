package com.usharik.app.ui.state

/** Rules shared by the full-declension session and its presentation. */
object DeclensionQuizRules {
    /** The visible mistake limit that triggers an interstitial and resets the per-word badge. */
    const val MAX_WRONG_ATTEMPTS = 10
}
