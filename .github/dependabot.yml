# Dependabot configuration for automatic dependency updates
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  # Gradle/Java/Android dependencies
  - package-ecosystem: "gradle"
    directory: "/"
    schedule:
      interval: "daily"
      time: "09:00"
      timezone: "Europe/Prague"
    open-pull-requests-limit: 10
    labels:
      - "dependencies"
      - "gradle"
    commit-message:
      prefix: "chore"
      include: "scope"
    # Group updates by type
    groups:
      androidx:
        patterns:
          - "androidx.*"
        update-types:
          - "minor"
          - "patch"
      google:
        patterns:
          - "com.google.*"
        update-types:
          - "minor"
          - "patch"
      testing:
        patterns:
          - "*junit*"
          - "*espresso*"
          - "*test*"
          - "*appium*"
        update-types:
          - "minor"
          - "patch"
      rxjava:
        patterns:
          - "io.reactivex.rxjava3:*"
        update-types:
          - "minor"
          - "patch"
      dagger:
        patterns:
          - "com.google.dagger:*"
        update-types:
          - "minor"
          - "patch"
      room:
        patterns:
          - "androidx.room:*"
        update-types:
          - "minor"
          - "patch"
      firebase:
        patterns:
          - "com.google.firebase:*"
        update-types:
          - "minor"
          - "patch"
    # Version constraints for specific dependencies
    ignore:
      # Ignore major version updates for stable dependencies
      - dependency-name: "androidx.lifecycle:lifecycle-extensions"
        update-types: ["version-update:semver-major"]

      # Pin AGP to 8.x - block major version updates (9.x)
      # Allow minor and patch updates within 8.x (e.g., 8.12.0 -> 8.12.1 or 8.13.0)
      - dependency-name: "com.android.application"
        update-types: ["version-update:semver-major"]
      - dependency-name: "com.android.library"
        update-types: ["version-update:semver-major"]
